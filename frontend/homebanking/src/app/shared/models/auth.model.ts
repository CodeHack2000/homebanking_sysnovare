/**
 * Represents the payload data for login and register requests, containing user email and password
 *
 * @export
 * @interface AuthModel
 */
export interface AuthModel {
    email: string;
    password: string;
};